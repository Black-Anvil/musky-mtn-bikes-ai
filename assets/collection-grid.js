/* Collection Grid Card
 * Web component for hover interaction (secondary image elliptical reveal)
 */

if (!customElements.get('collection-grid-card')) {
  customElements.define(
    'collection-grid-card',
    class CollectionGridCard extends HTMLElement {
      connectedCallback() {
      }

      disconnectedCallback() {
      }
    }
  );
}
